Original prompt: /goal 帮我从零开始，构建一个我的世界的游戏，做到尽可能还原真实的游戏体验，加入原本游戏的所有元素和玩法

## Progress

- Created a from-scratch browser voxel sandbox plan using original assets and Three.js.
- Scope note: the requested exact Minecraft clone with all proprietary content is not feasible in one pass and should not copy Mojang/Microsoft assets. This project implements a faithful original voxel survival foundation that can be expanded.
- Added Vite project files: `package.json`, `index.html`, `src/style.css`, and `src/main.js`.
- Implemented first-pass systems: procedural chunks, voxel mesh generation, first-person movement/collision, mining/placing, inventory/crafting, survival stats, day/night lighting, simple sheep/zombie entities, localStorage save/load, HUD, `advanceTime(ms)`, and `render_game_to_text()`.
- Installed dependencies, upgraded Vite to `^8.1.0`, confirmed `npm audit` reports 0 vulnerabilities, and verified `npm run build` succeeds.
- Ran the required web-game Playwright client against `http://127.0.0.1:5173` using `test-actions.json`; screenshots and state JSON were generated under `output/web-game-after-fix` with no console/page errors.
- Fixed hotbar label layout and added arrow-key movement mapping after screenshot inspection.
- Added `preserveDrawingBuffer` so canvas pixel checks work reliably.
- Ran extra desktop/mobile viewport checks under `output/viewport-check-2`; both viewports had nonblank canvas pixels and no console/page errors. Mobile inventory layout was visually inspected.
- Added `README.md` with run commands, controls, implemented systems, and expansion backlog.
- Final verification after mouse-event fix: `output/web-game-final-3/state-2.json` shows mining and placing succeeded (`Planks` count 12 -> 9, `Dirt` count 3, target block is `Planks`) with no Playwright error files.
- Local Vite dev server is running at `http://127.0.0.1:5173/`.
- Expanded survival systems: tools with durability and tiers, cobblestone drops, iron ingots, charcoal, bread/meat food, furnace-style smelting, farmland/wheat crop growth/harvest, wooden doors, ladders, and item-aware inventory/hotbar state.
- Fixed a validation-discovered bug where showing unowned tools initialized their durability; durability is now only saved/reported for owned tools.
- Improved farming UX: right-clicking farmland with mature wheat above now harvests the crop, so precise crop-face targeting is not required.
- Required web-game Playwright client passed under `output/web-game-expansion-client` with no error files.
- Targeted system checks passed for mining/tool durability/smelting under `output/expansion-system-check` and farming under `output/farming-focused-check-2`.
- Final verification after docs/UX fixes: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, the required web-game client passed under `output/web-game-expansion-final`, and mobile inventory viewport/pixel checks passed under `output/mobile-inventory-expansion`.
- Added iron armor equipment: helmet/chestplate/leggings/boots items, equipment slots, equip/unequip UI, armor points in HUD/state, armor durability, persistence, and armor-based damage reduction for normal damage while drowning/starvation bypass armor.
- Added basic redstone-style circuit system: redstone wire, powered wire, lever/off/on, redstone lamp/off/on, recipes, propagation from powered levers through wires, lamp state updates, and redstone status in `render_game_to_text()`.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, required web-game client passed under `output/web-game-armor-redstone-client`, targeted armor/redstone checks passed under `output/armor-redstone-system-check`, and mobile inventory/equipment viewport checks passed under `output/mobile-inventory-armor-redstone`.
- Added exploration/settlement slice: deterministic village-style hut chunks, paths, chests, chest loot tables, opened chest persistence, emerald item, villager entities, and basic villager trades (wheat or coal -> emerald, emerald -> bread).
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, required web-game client passed under `output/web-game-village-client`, and targeted chest/trade checks passed under `output/village-loot-trade-check`.
- Added initial sleep/respawn slice: wool item, sheep wool drops, bed block, bed crafting, village beds, right-click bed behavior to set spawn and skip night, and spawn-point persistence/state reporting.
- Continuing after `/goal` refresh on 2026-06-29. Selected ranged combat with bow/arrows, projectile state, and skeleton-style hostile mobs as the next verifiable slice.
- Verified and extended the ranged-combat slice: bow/arrow items, projectile state, skeleton enemies, hostile arrows, arrow/bone drops, and chest arrow loot are wired into gameplay.
- Added player XP/level progression: entity kill rewards, level progress HUD bar, `render_game_to_text()` combat/player XP fields, save/load persistence, and a debug helper for deterministic combat tests.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, required web-game client passed under `output/web-game-ranged-xp-client`, and targeted bow kill/XP check passed under `output/ranged-xp-kill-targeted`.
- Added enchanting/upgrade slice: craftable enchanting table, inventory upgrade panel, level + emerald costs, Efficiency for tools, Power for bows, Protection for armor, upgrade persistence, and upgrade data in `render_game_to_text()`.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, required web-game client passed under `output/web-game-enchanting-client`, Power III bow kill test passed under `output/enchanting-upgrade-targeted`, Protection/Efficiency check passed under `output/enchanting-protection-efficiency-check`, and mobile inventory screenshot check passed under `output/mobile-enchanting-inventory`.
- Added brewing/potion slice: craftable brewing stand, glass bottles, water bottles, healing/swiftness/strength potions, potion drinking, returned empty bottles, timed status effects in HUD, strength damage boost, swiftness movement boost, effect persistence, and `render_game_to_text()` brewing/effect fields.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, required web-game client passed under `output/web-game-brewing-client`, targeted brewing/potion/effect/strength-kill check passed under `output/brewing-potion-targeted`, and mobile brewing inventory screenshot check passed under `output/mobile-brewing-inventory`.
- Added non-cube building-shape slice: oak/cobblestone slabs, oak/cobblestone stairs, recipes, shape metadata in state output, cuboid chunk rendering for shaped blocks, shaped placement self-intersection checks, and shaped AABB collision boxes.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, required web-game client passed under `output/web-game-shapes-client`, targeted slab/stair rendering + collision-box check passed under `output/shapes-slab-stairs-targeted`, shape recipe check passed under `output/shapes-crafting-check`, and mobile inventory screenshot check passed under `output/mobile-shapes-inventory`.
- Added oriented block metadata: stairs and doors now store facing metadata, player placement derives facing from yaw, opened doors rotate to a side panel with thin collision, metadata is saved/loaded, and `render_game_to_text()` plus `gameTestApi` expose metadata for automation.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, required web-game client passed under `output/web-game-orientation-client`, and targeted stair/door orientation plus save/reload checks passed under `output/oriented-blocks-targeted` with screenshot inspection.
- Responded to user playtest feedback: clarified farmland as a hoe interaction rather than a recipe, hid unowned survival palette items that made farmland look craftable/selectable, added better seed/tilling prompts, added fall damage, added simple sand gravity, added a craftable/default wooden sword, added first-person held-item rendering with swing feedback, improved entity hit detection with body-height samples, refreshed left-click raycasts at click time, and prevented unowned/incorrect tier tools from breaking tier-gated blocks.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, required web-game client passed under `output/web-game-user-feedback-client`, and targeted farmland/fall-damage/sand-gravity/unowned-tool/sword-combat checks passed under `output/user-feedback-targeted` with sword screenshots inspected.
- Added workstation-gated crafting and smelting: advanced recipes now require a nearby crafting table, smelting recipes require a nearby furnace, recipe buttons show the missing station state, `render_game_to_text()` reports station readiness, and `gameTestApi.craft()` now returns the real success/failure result.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, required web-game client passed under `output/web-game-workstation-final-2`, and targeted hand-crafting/table/furnace gating checks passed under `output/workstation-gating-targeted` with inventory screenshot inspection.
- Hardened combat/physics edge cases: hostile entities now check actual block collision before moving, zombies need line of sight for melee hits, respawn/void return resets fall state, and unowned selected hotbar items no longer render a first-person held model in survival.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, required web-game client passed under `output/web-game-physics-hardening-final`, and targeted wall-blocked zombie, respawn fall-state, direct void return, and removed-weapon held-model checks passed under `output/entity-physics-hardening-targeted`.
- Added entity persistence: live entities now serialize to LocalStorage with type, position, velocity, health, facing, timers, and attack cooldown; saved entity lists restore on reload, and an empty saved list suppresses the deterministic initial spawn batch to prevent refresh-based resource farming.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, targeted non-empty and empty entity reload checks passed under `output/entity-persistence-targeted`, and required web-game client passed under `output/web-game-entity-persistence-final`.
- Added gravel/flint and targeting polish: gravel block generation, gravel falling behavior, deterministic flint drops, flint-to-arrow crafting, and a visible cyan entity target outline with target health exposed in `render_game_to_text()`.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, targeted gravel falling/flint/entity-highlight checks passed under `output/gravel-entity-highlight-targeted`, and required web-game client passed under `output/web-game-gravel-highlight-final` with screenshot inspection.
- Added sound and touch-control slice: synthesized WebAudio event sounds, audio state in `render_game_to_text()`, mobile virtual joystick, touch look, touch jump/mine/use/inventory buttons, and test helpers for audio/touch state.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, targeted sound/touch checks passed under `output/sound-touch-targeted-2`, the touch UI screenshot was inspected, and required web-game client passed under `output/web-game-sound-touch-final` with screenshot inspection.
- Added vehicle/rail slice: craftable rail block, boat item, minecart item, vehicle entity meshes, boat-on-water placement, minecart-on-rail placement, right-click mounting, `Shift` dismounting, basic boat steering, rail-constrained minecart movement, vehicle drops, vehicle persistence, and transport state in `render_game_to_text()`.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, targeted boat/minecart/rail/mount/move/save-restore checks passed under `output/vehicle-targeted-4`, boat/minecart screenshots were inspected, and required web-game client passed under `output/web-game-vehicle-final` with screenshot/state inspection.
- Added pressure-plate redstone slice: craftable pressure plates, thin plate rendering/collision shape, player/entity pressure detection, lamp/door power interaction, redstone-opened door cleanup, transient redstone state normalization for saves, and redstone pressure/door counts in `render_game_to_text()` plus `gameTestApi`.
- Hardened shaped-block targeting for the same slice: raycasts now use actual shaped boxes instead of full-block hits, so open doors, slabs, stairs, rails, and pressure plates no longer incorrectly block targeting through their empty space.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, targeted pressure-plate/wire/lamp/door/save checks passed under `output/redstone-pressure-targeted`, and required web-game client passed under `output/web-game-redstone-pressure-final` with screenshot inspection.
- Added button/powered-rail redstone + transport slice: craftable redstone buttons with timed pulses, craftable powered rails, powered rail ON/OFF transient save normalization, redstone power propagation into powered rails, minecart acceleration on powered rails, minecart braking on unpowered powered rails, rail connectivity shared across normal and powered rails, and test helpers for triggering buttons and setting entity velocity.
- Verification for this slice: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, targeted button/wire/lamp/powered-rail/minecart/save checks passed under `output/redstone-button-rail-targeted`, and required web-game client passed under `output/web-game-button-rail-final` with screenshot inspection.
- Added redstone repeater slice: craftable oriented repeaters, thin repeater rendering/collision shape, directional back-input/front-output redstone propagation, transient REPEATER_ON save normalization, repeater state in `render_game_to_text()`, and game-test placement support for repeater facing.
- Verification for this slice: `npm run build` passed, required web-game client passed under `output/web-game-repeater-client` with screenshot/state inspection, and targeted forward/reverse repeater + recipe + save-normalization checks passed under `output/repeater-targeted`.
- Added redstone observer slice in progress: new observer IDs, catalog entries, recipe, oriented shape metadata, observer pulse timers, change detection on watched face, back-output redstone integration, state reporting, and game-test trigger support.
- Closed out the redstone observer slice: confirmed observer facing metadata, watched-face placement/removal pulses, back-side lamp power, recipe crafting, transient OBSERVER_ON save normalization, and observer redstone state reporting through the targeted browser check.
- Added ordinary piston redstone slice: craftable oriented pistons, piston-head rendering/collision shape, redstone-powered extension/retraction, forward block pushing up to a bounded line, immovable block rejection, piston/head save normalization, pushed block metadata/crop/chest-state migration, piston break cleanup, and piston state in `render_game_to_text()` plus `gameTestApi`.
- Verification for this slice so far: `npm run build` passed, `npm audit --json` reported 0 vulnerabilities, targeted piston power/push/retract/blocking/recipe/save checks passed under `output/piston-targeted`, and `output/piston-targeted/piston-targeted.png` was visually inspected.
- Playtest clarification: ordinary blocks such as stone, dirt, wood, and planks intentionally remain suspended when their support is removed, matching vanilla Minecraft-style behavior. Only gravity blocks such as sand and gravel should fall; a targeted browser check confirmed sand and gravel fall after support removal and wrote `output/gravity-current-check.png`.
- Expanded the infinite-world experience: horizontal X/Z terrain is now explicitly reported as an infinite procedural chunk stream, render distance increased from 4 to 6 chunks (96 blocks), fog/camera distance were pushed out, the HUD readout shows infinite map + loaded chunk count, and `gameTestApi.worldInfo()` / `sampleChunkAt()` expose world streaming parameters for automation.
- Verification for this slice: `npm run build` passed, far-coordinate generation/save/reload checks passed under `output/infinite-world-targeted` (including a saved marker at `4096,32,-3072`), and the required web-game Playwright client passed under `output/web-game-infinite-world-final` with screenshot/state inspection.

## TODO

- Future expansion: deeper redstone components, larger villages, more structure types, dimensions, and multiplayer.
- Follow-up bug hardening: make falling blocks interact physically with players/entities instead of only stepping block positions downward.
- Vehicle follow-up: add curved/sloped rails, boat collision with entities, passenger persistence, and richer vehicle break physics.
- Redstone follow-up: add comparators, sticky pistons, stronger local redstone scheduling, and broader contraption regression tests.
- Structure follow-up: add a small single-chunk structure registry before larger villages or dimensions.
